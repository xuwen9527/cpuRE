#ifndef __CONTEXT_H__
#define __CONTEXT_H__

#include <bitset>
#include <glm/glm.hpp>
#include "framebuffer.h"

namespace cpuRE {
  template <
  struct BinRasterizaion {
    static int run(std::bitset<>, const glm::ivec2& binid, const glm::ivec4& bounds) {

    }
  };
}

#endif
